"""Single-agent orchestration package (Milestone 6).

`base` defines the Tool contract; `tools` holds the concrete tools and the ordered
PIPELINE; `orchestrator` runs them sequentially inside a job.
"""
